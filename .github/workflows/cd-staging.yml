name: CD — Deploy Staging (port 3001)

on:
  workflow_run:
    workflows: ["CI — Lint, Test, Build and Push"]
    types:
      - completed
    branches: [ staging ]
  workflow_dispatch:

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}

    steps:
      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            cd ~/Inventory-Management-System-MERN-CRUD-App

            # Pull latest code
            git pull origin staging

            # Log in to Docker Hub
            echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

            # Pull latest staging images
            docker pull ${{ secrets.DOCKER_USERNAME }}/ims-backend:staging
            docker pull ${{ secrets.DOCKER_USERNAME }}/ims-frontend:staging
            docker pull mongo:latest

            # Redeploy
            docker compose -f docker-compose.staging.yml down
            DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }} docker compose -f docker-compose.staging.yml up -d

            # Verify running containers
            docker ps
