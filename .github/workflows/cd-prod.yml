name: CD — Deploy Prod (port 3000)

on:
  workflow_run:
    workflows: ["CI — Lint, Test, Build and Push"]
    types:
      - completed
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy-prod:
    name: Deploy to Production
    runs-on: self-hosted
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Pull Latest Images
        run: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/ims-backend:prod
          docker pull ${{ secrets.DOCKER_USERNAME }}/ims-frontend:prod
          docker pull mongo:latest

      - name: Stop Old Prod Containers
        run: docker compose -f docker-compose.prod.yml down
        continue-on-error: true

      - name: Deploy Prod Containers
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        run: docker compose -f docker-compose.prod.yml up -d

      - name: Verify
        run: docker ps
